apiName: "Martine's API"

assets:
  mappings:
    /assets: /
  overrides:
    /: ../workshop-client/dist

server:
  rootPath: /api/*
  adminContextPath: /admin
  applicationConnectors:
    - type: http
      port: 81
  adminConnectors:
    - type: http
      port: 8081